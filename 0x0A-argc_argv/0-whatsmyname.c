#include <stdio.h>
#include "main.h"

/**
 * main - prints its name
 * @argc: counter
 * @argv: pointer to array of arguments
 * Return: 0 on success
 */

void main(int argc, char *argv[])
{
	printf("%s\n", argv[0]);
	return (0);
}
