#include "main.h"

/**
 * main - returns the length of a string
 * @s: string
 * Return: length
 */
int _strlen(char *s)
{
	int length;

	length = 0;
	while ((*str + length) != '\0')
		length++;

	return (length);
}
