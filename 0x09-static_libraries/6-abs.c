#include "main.h"

/**
 * _abs - computes the absolute value of an integer
 * @n: number argument
 * Return: integer
 */

int _abs(int n)
{
	if (n >= 0)
	{
		return (n);
	}
	else
	{
		return (-n);
	}

	_putchar('\n');
}
